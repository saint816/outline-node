# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与语义化版本。

## [1.2.0] - 2026-09-15

### Added

- 分隔线：`/divider` 将空节点变为横线、有文字节点变为居中标题横线；`---` + Enter 快速创建并继续输入。
- 横线支持添加标题、转为普通节点、拖拽、缩进、多选与原生撤销；Markdown 保存为列表内 `***`，带标题时使用备注续行，未编辑内容保持原样。

### Fixed

- 补全演示录制脚本的 ESLint 环境声明，恢复发布检查。

## [1.1.1] - 2026-09-02

### Fixed

- 修复代码块节点被异常渲染的严重 bug：节点备注是围栏代码块时，代码块内以 `- ` / `1.` / `- [ ]` 开头的行（含列表语法的代码内容）会被 parser 误判为新列表节点，把代码块拆成散落的节点与文本块。现在围栏已开启时这些行按代码内容完整收进代码块（回归测试：`code-block-with-list.md`）。

## [1.1.0] - 2026-09-02

### Removed

- 移除 Numbered List（有序列表）功能：`/` 菜单不再提供 Numbered 条目，`Cmd/Ctrl+Shift+7` 不再切换有序列表，`toggleOrdered` op 从协议与实现中删除。
- 已有 `1.` / `1)` 编号行不再渲染编号（显示为普通圆点节点）；未编辑的文件内容保持字节原样，编辑后按普通 bullet（`- `）序列化。

## [1.0.3] - 2026-08-31

### Fixed

- 侧栏 Starred 分区的书签现在可展开/折叠，查看其子节点结构（星标书签仍不可拖拽、不作为拖拽落点）。
- 点击节点圆点进入 zoom 后，光标落到标题行尾；此时按 Enter 会在该节点下新建子节点，而不是在同级创建兄弟节点。

## [1.0.2] - 2026-08-15

### Added

- 新增 `Cmd/Ctrl+Shift+Backspace` 快速删除当前节点或多选节点；删除范围包含后代时显示二次确认，整次操作仍只占一个 VS Code undo 步。
- 超过 240px 的代码块默认收起，可展开/收起，编辑聚焦时自动展开；状态仅保存在 Webview UI state。
- 链接工具新增标题与 URL 双输入框；编辑态 Markdown 源码通过 CSS Highlight 标出行内代码、加粗、高亮与链接语法，不改变 `contenteditable` DOM。

### Changed

- 空父节点行首 `Backspace` 会按原顺序提升其直接子节点，再删除空父节点。
- 节点代码块改为标题必填且标题始终显示在代码块上方；`/code` 与纯围栏创建入口保持可用，空标题创建后立即聚焦可编辑的必填标题行，旧空标题文档同样提示但不自动改写文件。
- 侧栏导航会把焦点送到右侧 zoom 根正文；正文折叠按钮扩大到至少 24×24px，节点圆点拖拽期间禁止原生文本选择。
- `/` 插入菜单移除 To-do 入口，只保留 Code 与 Numbered List；已有任务节点与完成操作保持兼容。

### Fixed

- 修复从侧栏进入节点后无法立即用 `Shift+↓` 多选其子节点的问题。

## [1.0.1] - 2026-08-01

### Changed

- 移除产品元数据、Marketplace 关键词、README、规格、源码注释、测试与历史文案中的第三方产品品牌关联；OutlineNode 统一定位为本地优先、基于纯 Markdown 的独立大纲编辑器。功能与文件格式不变。

## [1.0.0] - 2026-08-01

OutlineNode 进入 1.0：纯本地 Markdown 大纲编辑、折叠与 zoom、搜索、任务、备注、拖拽、镜像、图片、代码块、行内 Markdown、多选和原生 Undo/Redo 已形成完整闭环。本版同时修复发布前 GUI 回归发现的三个数据展示与历史顺序问题。

### Fixed

- **折叠节点无法从侧栏正常 zoom**：zoom 根现在在当前页面强制展开，因此即使它在 Home 视图中处于折叠状态，右侧仍会显示子节点；退出 zoom 后原折叠状态不变。
- **Starred 与 Home 重复高亮**：同一节点同时出现在两个分区时，只高亮实际点击进入的那一份；正文、面包屑或历史导航进入时默认高亮 Home 中的规范位置。
- **外部刷新后立即编辑再 Undo 可能删除外部内容**：host 现在严格串行处理 Webview 消息，确保 `requestUndo` 必须等待前一条 `workspace.applyEdit` 进入 VS Code 原生 undo 栈，只撤销用户刚做的 Webview 编辑。

### Changed

- 升级 `@vscode/test-electron` 至 3.1.0，兼容新版 macOS VS Code 将测试宿主可执行文件从 `Electron` 改名为 `Code`；发布前集成测试可继续在当前稳定版 VS Code 上运行。

## [0.10.0] - 2026-07-28

行内 Markdown 终于「所见即所得」：代码、加粗、高亮、链接直接渲染成效果，点进去才看到记号。

### Added

- **行内 Markdown 渲染**：`` `行内代码` ``、`**加粗**`、`==高亮==`、`[文字](url)` / `<url>` / 裸链接（`https://…`、`www.…`）现在直接渲染成效果，记号隐藏。**点进去自动回源码态**看到完整 Markdown，编辑、光标、IME 全走原来的路；失焦再变回显示态。数据层只存原始 Markdown，不改文件格式、不新增数据种类。
- **选区排版工具条**：选中文字即在上方浮出小工具条——加粗 / 高亮 / 行内代码 / 链接，点一下加、再点一下取消。**刻意做成工具条而不是只给快捷键**：webview 的按键会转发给工作台做绑定解析，`preventDefault` 拦不住 VS Code 自己的 `Cmd+B`（切换侧边栏），本项目已在键位上实机栽过两次。
- **排版快捷键**：`Ctrl+B` 加粗 / `Ctrl+H` 高亮（macOS），其他平台是 `Ctrl+Alt+B` / `Ctrl+Alt+H`——那两个裸键被「切换侧边栏 / 全局替换」占着。帮助浮层（`?`）已收录。
- **选中文字时粘贴链接 → 直接变成 `[选中的字](url)`**。判据从严：只认单个 token 且带 `http(s)`/`mailto` 协议或 `www.` 前缀的文本，普通文字、带空格的串、裸域名（`example.com`）一律照常粘贴——宁可漏判，也不能把选中的字吞掉。选中的整段本身已是链接时，粘贴会**换掉地址**。
- **点击链接用系统默认程序打开**（新增单向协议消息 `openLink`）。host 侧只放行 `http` / `https` / `mailto`，`vscode:`、`file:`、`javascript:` 等一律拒绝——正文里的 url 是不可信输入。按住 `Alt` / `Cmd` / `Ctrl` 点击则照常进去改字。

## [0.9.0] - 2026-07-26

0.8.0 实机反馈的一轮修补，全部围绕代码块：**能切换语言、能缩进、有圆点能拖**，以及三个观感/焦点缺陷。

### Changed

- **新建的代码块一律挂到节点下，不再变成「文档级代码块」**：在空的顶层节点上打 ` ``` ` 或 `/code`，此前会把该节点整个换成文档级代码块——那种块**没有圆点，拖不动、缩进不了、也选不中**（实机反馈）。这是 0.4.0 代码块还不能嵌套时的权宜之计，0.7.0 起已无必要。文件里本来就写在顶层的围栏块仍按文档级块渲染（那是 Markdown 的事实结构，不是 UI 选择）。

### Added

- **代码块可切换语言**：点右上角的语言徽标选语言（12 项 + 纯文本），围栏开行随之重写。此前围栏行被渲染层摘掉，语言只活在 DOM dataset 里，**改不了**。切换保留原缩进与围栏字符（``` / ~~~ 及其长度）。
- **代码块里 `Tab` / `Shift+Tab` 缩进该节点**：代码块节点的正文宽度为 0、点不到，此前 Tab 只是把焦点甩走，等于没有缩进入口。顶层代码块没有层级，`Tab` 改为插两个空格（默认行为同样是甩焦点）。

### Fixed

- **代码块里按 `Esc` 冒出一条空输入框**：`Esc` / 首行 `↑` 原本无条件回「本节点正文」，而代码块节点的正文是空的、零宽，聚焦它就在代码块旁边多出一个空框。正文为空时改去相邻节点；正文有字仍回正文。顺带把行内代码块挪到正文之后挂载——正文获得焦点时代码块折到下一行并缩进对齐，而不是在它下面冒空框。
- **代码块头部占掉一行**：0.8.0 新加的语言徽标 + 复制按钮把「块排进节点行」的效果顶掉了，代码块上方又冒出一条空行。头部改为浮在代码块右上角，不占布局高度。
- **高亮层取语言取错**：`syncHighlight` 从徽标文字读语言，而徽标显示的是本地化后的「纯文本」，不是语言 id。

## [0.8.0] - 2026-07-26

代码块从「能用」推到「好用」：**语法高亮 + 一键复制 + 出口手势**；侧栏按实机反馈重做观感并支持分区折叠；图片节点补上键盘出口。

### Changed

- **侧栏观感重做**：当前位置从通栏高亮色块改为「淡底 + 左侧 2px 竖条 + 字重」；分区标题不再全大写、改用留白分组；行高放宽到 26px；星标与折叠三角默认低透明、hover 才提亮；收起按钮改用箭头；层级除缩进外再叠一层明度差。纯样式/渲染层改动，不碰协议与数据模型。

### Added

- **代码块语法高亮**：按围栏语言着色，**编辑时也保持高亮**——高亮层是 textarea 背后的一层 `<pre>`，两层严格对齐。当前支持 js / ts / json / python / bash / go / rust / sql / yaml / html(xml,svg,vue) / css（+常见别名），识别不了的语言照常当纯文本编辑，功能不受影响。配色走主题变量，明暗主题自适应。高亮库打进 bundle（CSP 不允许外链），webview 体积 81KB → 122KB。**语言集是按性能红线定的，不是随手挑的**：16 门全装时 5000 节点的「外部 refresh patch」5 次采样全部 ≥49ms（红线 50ms），现在这套均值 45.5ms、与不装高亮的基线 46.9ms 无差别。要加语言必须先单独串行复测 perf。
- **代码块复制按钮**：hover 代码块出现，点一下整块代码进系统剪贴板并显示「已复制」。走宿主 `vscode.env.clipboard`（新增单向协议消息 `copyText`），不用 webview 里的浏览器剪贴板 API——那些 API 在 VS Code webview 下有静默失败的前科。
- **代码块的出口手势补齐**：代码块内 `Esc` 回到本节点正文、**末行 `↓`** 去下一个节点、**首行 `↑`** 回正文（此前只有 `Cmd/Ctrl+Enter` 新建同级节点一条路，不知道这条手势就会卡在里面出不来）。同时代码块 textarea 内的方向键/回车不再漏给大纲 keymap——它不认识 textarea 的行结构，光标停在代码块中间行时会被弹到别的节点去。
- **关闭图片放大浮层后光标回到该图片所在节点**：点图 → 预览 → `Esc`，接着按 `Enter` 就能建同级节点。此前关掉浮层焦点就悬空，图片节点等于没有键盘出口。
- **侧栏分区可折叠**：`Starred` 标题与 `Home` 行即开关，折叠状态随 `ViewState` 持久化。收起 `Starred` 后同一节点不再在两区各列一次。
- **`Home` 合并成大纲区的标题行**：不再「顶上一行 Home + 下面一个 Outline 小标题」两处重复——三角折叠整棵树，文字点击回到全文档，大纲树缩进到它之下。

### Fixed

- **侧栏不再弹出无效的原生右键菜单**（Cut / Copy / Paste 在纯导航面板里都是空操作）。
- **展开侧栏节点时整行闪烁**：侧栏每次 render 整栏重建，行上的 hover 过渡会随重建从头重播。侧栏内一律去掉 hover 过渡，并在重建后把焦点送回刚点的开关。

## [0.7.0] - 2026-07-25

大纲体验的一大步：**节点多选**、**代码块可以挂在任意节点下**（此前只能顶层，实际用起来够不着）、**图片体验四项**。同时按实机反馈改掉编辑态外边框、zoom 标题对齐与隐藏已完成的键位。

### Added

- **节点多选**：`Shift+↑/↓` 进入并扩展选区（判据与 `↑/↓` 跨节点移动一致，节点内还能扩文本选区时不抢），`Shift+点击`选一段。选中后 `Tab` / `Shift+Tab` 批量缩进、`Alt+↑↓` 批量移动、`Cmd/Ctrl+Enter` 批量完成、`Backspace` 批量删除、`Cmd/Ctrl+C/X` 整段复制为一份 markdown 列表。一次批量走 `store.dispatchAll` 合成**一条 edit 消息 = 一个 undo 步**，`Cmd+Z` 整体撤销。纯 webview 状态：不写文件、不改协议、不新增 op（`outdent`/`moveDown` 必须逆序施加，否则选中项相对顺序会颠倒）。
- **代码块可挂在任意节点下**：此前代码块只能是顶层文档级块——而大纲里几乎所有能落笔的空节点都是嵌套的，等于"设计上支持、用起来够不着"。现在在任意节点上打 ` ```lang `+回车 或 `/code`，代码块挂到该节点下（文件里就是缩进在该列表项内容列下的围栏块，Obsidian / Logseq 原样渲染）。**不需要新 op、不改数据模型、不改协议**：这种块本来就被 parser 解析成该节点的 `note`，缺的只是渲染与创建入口。
- **图片点击放大预览**：点任意预览图弹出全屏浮层，点浮层或 `Esc` 关闭。
- **清理未被引用的粘贴图片**：新命令 `OutlineNode: 清理未被引用的粘贴图片`（确认 + 移废纸篓），以及**保存后自动清理**（设置 `outlineNode.cleanupUnusedImagesOnSave`，默认开；只动扩展自己生成的 `pasted-*`、移废纸篓）。刻意**不做「删节点即删文件」**：删节点可 undo、删文件不可，一耦合就是「撤销回来正文在、图没了」这种 undo 修不好的损失；延到保存则给 undo 留出窗口。

### Changed

- **粘贴的图片改存到 `<文件名>/assets/`**（如 `notes.outline.md` → `notes/assets/pasted-….png`），不再撒在笔记同级目录。已有的旧图仍在原位照常显示，无需迁移。协议字段形状未变，`saveImage.name` 从纯文件名放宽为相对路径（host 侧逐段白名单校验，仍挡路径穿越）。
- **图片节点不再显示源码**：正文只有图片语法时隐藏 `![[…]]` 源码、只留图，聚焦即回源码态继续编辑。
- **块内容排进节点行**：节点内容只有图片或只有代码块时，块直接排在 bullet 右边——此前块上方会多出一条只有 bullet 的空行。
- **编辑节点时不再画焦点外边框**：闪烁的光标已经说明焦点在哪，再套一圈边框会让整页在打字时不停跳框。高对比度（forced-colors）主题下仍保留焦点提示。
- **隐藏 / 显示已完成的键位**：`Cmd+O` → `Ctrl+O`（macOS）/ `Ctrl+Alt+O`（Windows、Linux）。webview 的按键会被转发给工作台做快捷键解析，只能挑 VS Code 没占的组合——`Cmd+O` 撞「打开文件」、`Cmd+Alt+O` 撞 Remote 扩展的「Open Remote Window」，都实机否掉过。平台由 host 注入，不嗅探 UA。

### Fixed

- **代码块里的空行会把它截断**：`- 节点` 下缩进的围栏代码块，正文一有空行，重新解析时 note 就从空行处断掉、模型被拆成两半（文件字节不丢，但 UI 上代码块断成两截）。parser 现在跟踪 note 内的围栏状态，未闭合期间空行照收；不缩进的行仍照旧终止，未闭合围栏不会吞掉后面的标题 / 正文。
- **zoom 根标题的对齐**：标题首字现在与子节点圆点左缘对齐（此前 bullet 占位把标题挤得比自己的子节点还靠右）。

## [0.6.0] - 2026-07-24

依据 0.5.0 真机 GUI 测试报告（`docs/11`）修掉 4 个 bug，并给代码块补上「删除」「后接节点」两个出口，解掉它作为文档级块的死胡同。第 5 个报告项（undo 跨多步）经集成测试核实**不复现**——独立 edit 消息各自成一个 undo step。

### Added

- **代码块出口手势**：代码块此前是死胡同（只能顶层、无法删、后面进不去）。现在——代码块 textarea 内 **Cmd/Ctrl+Enter** 在其后新建顶层节点并聚焦（`insertRootAfterBlock` op）；**空代码块上 Backspace/Delete** 删掉整个围栏代码块、焦点落到相邻节点（`deleteRawBlock` op，只删围栏块、删后合并相邻列表）。帮助浮层（`?`）已收录这两条。

### Fixed

- **隐藏已完成的焦点陷阱**：焦点在完成节点上按 `Cmd/Ctrl+O` 隐藏后，该节点被 `display:none`、焦点掉到 `body`，同一快捷键无法再显示。现改为 document 级监听（焦点在哪都触发）+ 隐藏后把焦点迁到最近的可见节点。
- **粘贴图片不显示（渲染层）**：0.5.0 修好了引用插入，但预览 `<img>` 的请求早于图片写盘 → 404 被浏览器缓存，`imageSaved` 重渲染时又因 `src` 未变不重建 img → 图始终空白。现在落盘回执到达时给对应 img 打 cache-bust 强制重新拉取。

## [0.5.0] - 2026-07-24

斜杠插入菜单 + 三个 0.4.0 实机 bug 修复。其中图片粘贴此前**完全失效**（图片写了盘、但正文没插入引用，什么都不显示），本版修好。

### Added

- **斜杠插入菜单（`/`）**：正文词首输入 `/` 弹出可过滤菜单——**Code block**（受纯 Markdown 红线，仅顶层空节点）、**To-do**、**Numbered list**。↑↓ 选、Enter/Tab 确认、Esc 忽略当前 token、光标移出/失焦即关。条目复用现有 op、不改数据模型；为「未勾选任务」新增附加 op `setChecked`（`toggleChecked` 从 `null` 只能到 `[x]`，表达不了 `[ ]`）。帮助浮层（`?`）新增 `/` 一行。

### Fixed

- **图片粘贴完全失效**：粘贴图片曾**写盘成功但正文不插入 `![[name]]` 引用**，导致什么都不显示。根因是 `execCommand('insertText')` 在 VS Code 的 webview 里对 `contenteditable="plaintext-only"` 静默失败（Playwright 里却能用，测试一直被骗过）。改经 store 确定性插入，回归断言下沉到协议层。
- **首节点删不掉**：空的首节点（无前驱）按 Backspace 现在删除并把光标移到下一个可见节点；非空首节点仍不删（不丢正文），文档仅剩一个节点时不删。
- **侧栏打字不实时刷新**：编辑节点文本时侧栏 label 现在实时跟上（`syncText` 只改同名 label，不碰打字热路径——击键红线 0.04ms 未受影响）。

## [0.4.0] - 2026-07-24

编辑体验一轮打磨：代码块从只读升级为可创建 / 可编辑的一等公民、侧栏支持拖拽移动、节点内可直接粘贴图片、原生 ↔ 大纲双向切换入口，外加一个 note bug 修复。数据模型改动（代码块可编辑）已先落 `docs/02` + `docs/04` 并评审。

### Added

- **代码块可创建 / 可编辑**（路线 B）：围栏代码块正文改为可编辑 `textarea`（`setRawBlock` op，保留首尾围栏字节）；在**空的顶层根节点**上打 ` ``` ` / ` ```lang ` 回车即转成代码块（`toCodeBlock` op）。代码块在纯 Markdown 模型里始终是文档级块，**只能在顶层、不能嵌套**（见 `docs/02`）。其余 RawBlock（frontmatter / 标题 / 正文 / 图片）仍严格不可变。
- **侧栏拖拽移动节点**：左侧大纲树的行可拖拽重排 / 改嵌套，复用主编辑区的 `move` op（自带跨 block、成环兜底）。纯 UI，不改文件。
- **节点内粘贴图片**：光标处 `Ctrl/Cmd+V` 粘贴图片 → 就地插入 Obsidian 原生 `![[name]]` 并把图片写到文档同目录（`saveImage` 协议，文件名带路径穿越防护）。渲染复用既有图片预览。
- **原生 ↔ 大纲双向切换入口**：命令 `OutlineNode: Open as Markdown Text` / `Open as Outline`，出现在编辑器标题栏按钮、命令面板与 `.md` 资源管理器右键菜单（走 i18n）。普通 `.md` 仍默认用原生文本编辑器打开，Outline 是可选项。

### Fixed

- 给节点加 note 后清空、再失焦，残留空行不再存在——空 note 归一为 `null`；顺带修掉一个文件层问题：`note:''` 曾被序列化成一行空白写进 `.md`。
- 清理 `nodeView.ts` 里一个混进源码的 `\x00` 字节（代码块变更检测的分隔符改回普通空格；纯内部，无行为变化）。

## [0.3.0] - 2026-07-24

富节点类型第二阶段：代码块渲染 + 有序列表（Number 类型）。字节 round-trip / 最小 diff 红线由扩展后的 property 测试守护（有序项也纳入 fuzz）。

### Added

- **代码块渲染**：围栏代码块（```` ``` ````/`~~~`）渲染为独立代码块——隐藏围栏、显示语言标签、等宽代码样式。只读，原文字节仍原样保留。
- **有序列表（Number 类型）**：`1.` / `1)` 解析为一等公民有序项，bullet 位显示编号；`Cmd/Ctrl+Shift+7` 在普通 / 有序间切换。刻意不自动重排编号（保最小 diff），Obsidian 打开照常按首项自增渲染。
- **侧栏大纲树可展开/折叠**：左侧栏大纲区改为可展开树，每个有子节点的条目前带 ▸/▾ 三角；侧栏展开状态独立于主编辑区折叠（只存内存，不落文件）。

### Changed

- 围栏代码块现在各自独占一个 RawBlock（渲染层据此处理；字节仍原样保留，round-trip 不变）。

## [0.2.0] - 2026-07-24

大纲体验增强 + 富节点类型第一阶段。所有新状态只进内存 / workspaceState，绝不写进用户的 `.md`；图片与 i18n 是纯渲染 + 附加协议/HTML 注入，不改文件格式与数据模型。

### Added

- **内嵌左侧栏**：列出当前文档顶层节点 + 星标书签，点击即 zoom；可收起。星标存 workspaceState（按 nodeKey，跨会话稳定），绝不写进 `.md`。
- **顶部工具条**：zoom 前进 / 后退历史、隐藏已完成开关、快捷键帮助入口。
- **隐藏已完成**（`Cmd/Ctrl+O`）：已完成节点连同整棵子树收起，与搜索过滤并存。纯 UI 状态。
- **快捷键帮助浮层**（`?`）。
- **图片渲染**：节点正文里的 `![[img.png]]` / `![](img.png)` 显示为预览图，独立成行的图片渲染为图片块。相对路径按文档所在目录解析；镜像块引用 `![[#^id]]` 不误判为图片。源码仍留在正文里可编辑。（Obsidian 全库图片解析暂不支持。）
- **真正的 i18n**：UI 跟随 VS Code 显示语言（简体中文 / 英文），默认英文，与商店页一致。

### Changed

- 完成态改为简洁样式：已完成节点只显示灰色删除线，不再画 checkbox 方框。纯渲染改动，文件层仍是 `- [x]` / `- [ ]`，Obsidian 打开照常渲染勾选框。

## [0.1.1] - 2026-07-24

### Changed

- README 默认语言改为英文（Marketplace 门面与 GitHub 首页），中文版保留在 `README.zh-CN.md`，两边互相链接。

## [0.1.0] - 2026-07-23

首个公开版本。

### Added

- **大纲编辑器**：`*.outline.md` 默认打开；普通 `*.md` 可通过 Reopen With 或 `OutlineNode: Open as Outline` 打开。
- **结构编辑**：`Enter` 拆分、`Tab` / `Shift+Tab` 调整层级、`Backspace` 合并、`Alt+↑/↓` 移动整棵子树、鼠标拖拽跨层级排序。
- **折叠 / 展开**（`Cmd/Ctrl+.`）：折叠状态存在 VS Code 里，不写入文件；文件被外部修改后仍能对上原来的节点。
- **Zoom-in**（`Alt+←/→`）：聚焦任意子树并显示面包屑，窗口重开后自动恢复。
- **完成标记**（`Cmd/Ctrl+Enter`）：序列化为标准的 `- [x]`。
- **节点备注**（`Shift+Enter`）：按缩进续行存储，支持多行。
- **实时搜索过滤**（`Cmd/Ctrl+F`）：保留祖先链，搜索时穿透折叠。
- **剪贴板**：粘贴缩进列表自动还原层级，复制 / 剪切输出标准 Markdown 子树。
- **镜像引用**：用 Obsidian 原生 `^id` / `![[#^id]]` 块嵌入语法，同一节点可出现在多处并同步编辑；断链与循环引用降级为只读提示，绝不静默删除原文。
- **中文 IME**：合成期间不触碰编辑中节点的 DOM。
- **撤销 / 重做**：直接转发给 VS Code 原生 undo 栈，与文本编辑器共用一套历史。

### 保证

- **字节级 round-trip 幂等**：未编辑的行原样保留，包括缩进单位、行尾（CRLF/LF）、frontmatter、代码块和非列表内容。
- **最小 diff**：一次编辑只改对应的那几行，不重排整个文件。
- **UI 状态不落盘**：折叠、zoom、搜索一律存在 VS Code 里。
- 性能红线（5000 节点基准，随 CI 跑）：解析 < 20ms、首帧 < 500ms、击键 JS < 16ms、外部改动增量 patch < 50ms。
